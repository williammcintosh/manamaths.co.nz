\documentclass[17pt,a4paper,landscape]{extarticle}
\usepackage[margin=2.35cm,top=0.12cm,left=1.05cm]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tasks}
\usepackage{tikz}
\usepackage{xcolor}
\usepackage[sfdefault,lf]{FiraSans}
\renewcommand{\familydefault}{\sfdefault}
\setlength{\parindent}{0pt}
\pagestyle{empty}
\settasks{label=\textbf{\arabic*.}, label-width=2.2em, item-indent=2.95em, column-sep=2.1cm, after-item-skip=5.3em}
\renewcommand{\arraystretch}{1.15}
\everymath{\displaystyle}

\begin{document}
\sffamily
\boldmath

\vspace*{0.4em}
{\LARGE \textbf{Excellence}}
\begin{tasks}(2)
\task A student says when \mbox{$x=2$}, \mbox{$x^3+x=10$}. Are they correct? Explain.
\task When \mbox{$x=1$} and \mbox{$y=2$}, evaluate \mbox{$2x+3y$} and state which is larger.
\task Substitute \mbox{$x=3$} into \mbox{$2x^2+1$} and show working.
\task When \mbox{$p=5$}, simplify \mbox{$2(p-1)+3$}.
\task If \mbox{$x=4$} and \mbox{$y=-1$}, evaluate \mbox{$3x+2y^2$}.
\task Substitute into brackets with powers: when \mbox{$x=2$}, evaluate \mbox{$(x+1)^2$}.
\task Use substitution to evaluate \mbox{$3x+2^2$} when \mbox{$x=4$}.
\task When \mbox{$a=3$} and \mbox{$b=2$}, simplify \mbox{$a^2+b\times4$}.
\task Explain when substitution must respect order of operations.
\task Create one challenge substitution question and provide its answer.
\task Explain how substitution can be used to check an algebraic solution.
\task Evaluate \mbox{$x^3+x$} when \mbox{$x=-1$} and interpret the result.
\end{tasks}

\end{document}
